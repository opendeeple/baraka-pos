// Design tokens come from @baraka/ui-tokens — the single source of truth
// shared with mobile-office and the desktop Tailwind config.
export { colors, spacing, radius, type, touch, motion, opacity, breakpoints } from '@baraka/ui-tokens'
